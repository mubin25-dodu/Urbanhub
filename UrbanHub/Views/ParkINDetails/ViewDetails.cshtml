@* @{
	ViewBag.Title = "Details";
	Layout = "_LayoutParkIN";
}
<partial name="alart" />

<div class="col">
	<div class="card h-100 shadow-sm border-0 rounded-4 overflow-hidden">
		<img class="card-img-top" src='@(string.IsNullOrEmpty(i.Image) ? "/assets/img/Broken.png" : i.Image)' alt="Parking spot image" style="aspect-ratio: 16 / 10; object-fit: cover;">
		<div class="card-body p-3">
			<div class="d-flex justify-content-between align-items-start mb-2">
				<div>
					<h5 class="card-title mb-1 fw-semibold">@i.Address</h5>
					<p class="text-muted small mb-0">Rent/Hour @i.RentPerHour BDT</p>
				</div>
				<span class="badge text-bg-success rounded-pill">@(i.Distance == 0 ? "open" : i.Distance + "Km Straight")</span>
			</div>
			<p class="card-text text-secondary">@i.Description</p>
			<a class="btn btn-outline-primary rounded-pill px-3" 
			   asp-controller="ParkINDetails" asp-action="ViewDetails" asp-route-image="@i.Image" asp-route-id="@i.ID">
				<i class="fa-solid fa-circle-info"></i>&nbsp;View details
			</a> &nbsp;
							

			<a href='@(i.Location == null ? "#" : $"https://www.google.com/maps/dir/?api=1&destination={i.Location.Y},{i.Location.X}&travelmode=driving")'
			   target="_blank"
			   class="btn btn-outline-primary rounded-pill ml-3 px-3">
				<i class="fa-solid fa-map"></i>&nbsp;View On Map
			</a>
		</div>
	</div>
</div>

@section Scripts {
	@Html.Partial("_ValidationScriptsPartial")
} *@